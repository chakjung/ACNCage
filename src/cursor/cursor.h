#pragma once

/**
 * Create listeners for cursor events
 * :param server: server hosting the listeners
 * :return: Success 0, Error -1
 */
int ACNCageServer_CreateCursorListeners(struct ACNCageServer* server);
